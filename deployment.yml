---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kittu
spec:
  replicas: 3
  selector:
    matchLabels:
      app: flip
  template:
    metadata:
      labels:
        app: flip
    spec:
      containers:
      - name: tetris
        image: ramakrishna737:pflipcart:v1
        ports:
        - containerPort: 300
  
